spring:
  application:
    name: mcp-kotlin-sample
  threads:
    virtual:
      enabled: true
  ai:
    mcp:
      server:
        name: mcp-kotlin-sample
        version: 1.0.0
        protocol: STREAMABLE

server:
  port: 8080
